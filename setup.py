import os
from setuptools import setup, find_packages

setup(name='tensorflow2timeloop',
        version='0.1', 
        url='https://github.com/Haimrich/tensorflow2timeloop',
        install_requires=[],
        python_requires='>=3',
        include_package_data=True,
        packages=find_packages())